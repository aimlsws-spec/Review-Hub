# Core service — required.
fastapi==0.141.1
uvicorn[standard]==0.52.4
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3

# OCR (screenshot/proof verification) — degrades gracefully if the Tesseract
# binary itself isn't installed on the machine (see README.md). pytesseract
# is just a thin wrapper; Pillow does the actual image loading.
pytesseract==0.3.13
pillow==12.3.0

# LLM-backed features (Review Assistant, Caption/Story Generator) — talks to
# a locally-run Ollama instance (free, no API key) via its OpenAI-compatible
# endpoint. Degrades gracefully if Ollama isn't running (see README.md).
# No paid provider is configured or required anywhere in this service.
langchain==1.3.16
langchain-openai==1.6.0
